```console
./picSort.py -h

usage: picSort.py [-h] -sf SF -df DF

A Script to sort JPG picutures based on their exif metadatas DateTimeOriginal tag

optional arguments:
  -h, --help  show this help message and exit
  -sf SF      This folder needs to be sorted
  -df DF      Destination folder for sorted pictures

```
